# Лекция 4. Какие решения будет дорого изменить позже?

> **Производный материал.** Глава конспекта лекций для самостоятельного чтения, оформленная по вузовскому шаблону курса. Содержательная модель — в [model.md](model.md), сценарий — в [script.md](script.md).

## Краткие тезисы лекции

- Архитектура — это совокупность решений, дорогих в изменении, а не диаграмма компонентов или выбор технологий.
- Архитектурно значимое решение может быть одной строкой внутри обычной функции — размер и внешний вид кода не выдают его значимость, и не важно, кто её написал: человек или AI.
- Признаки значимости: решение трудно локально отменить, влияет на несколько частей системы, а его ошибка воспроизводится везде, где решение используется.
- Граница между компонентами — это граница ответственности: что один компонент обязан гарантировать другому.
- Сравнение архитектурных альтернатив требует явных свойств качества, так же как сравнение решений в целом требует явных критериев.
- Запись архитектурного решения (ADR) делает решение видимым — но не делает его автоматически обоснованным: доменная гипотеза, даже явно записанная, остаётся гипотезой, пока её не проверит специалист.
- Эволюционность — не то же самое, что проектирование про запас: важно осознанно выбрать, какие решения оставить дорогими, а какие сделать легко пересматриваемыми.
- AI, попрошенный проверить уже написанный человеком код, способно обнаружить архитектурно значимое предположение — и честно назвать предел своей компетенции там, где нужна оценка специалиста, а не инженера.

## Теоретический материал

### 4.1. Ситуация: код, написанный в спешке, и ревью, которое его находит

По итогам лекции 3 команда определила первый инкремент: сопоставление жалоб жителей с показаниями датчиков и станций, с явными границами и проверяемым критерием приёмки. К пятнице нужен рабочий пилот — один из участников команды пишет код сам, быстро.

Код компилируется, проходит приложенный тест, читается легко. Полный текст — в [`assets/pilot_code_review.md`](assets/pilot_code_review.md) (часть 1).

Прежде чем читать дальше, посмотрите на этот код так, как если бы вы должны были принять его в работу. Задайте себе вопрос: есть ли в нём решение, которое будет дорого исправить через месяц, если добавится новый источник данных?

Внимательное чтение обнаруживает функцию `to_comparable_scale`, которая молча переводит показания VOC-датчика в «условный эквивалент PM2.5» по коэффициенту `0.12` — без обоснования, без ссылки на источник, без покрытия тестом. Перед показом специалисту команда просит AI сделать ревью кода на предмет скрытых архитектурных предположений. AI находит именно эту строку, отмечает непокрытую тестом ветку и честно добавляет: «я не могу сказать, верен ли коэффициент — это вопрос предметной области». Полное ревью — в [`assets/pilot_code_review.md`](assets/pilot_code_review.md) (часть 2).

Это исходное напряжение лекции: **архитектурно значимое решение не обязано выглядеть как архитектурное решение — оно может быть одной строкой внутри функции, написанной человеком в спешке, — и именно поэтому его легко не заметить, пока кто-то, человек или AI, не посмотрит на код именно с этим вопросом.**

### 4.2. Архитектура — это решения, а не диаграмма

Диаграмма компонентов, выбор фреймворка, схема развёртывания — способы зафиксировать и передать архитектурные решения, но не сама архитектура. Архитектура — совокупность решений о системе, изменение которых требует существенных усилий: как система разделена на части, как части взаимодействуют, какие предположения заложены в границы между ними.

Архитектурное решение может быть принято в любом месте — в схеме базы данных, в структуре модуля, в единственной функции нормализации данных — а не только в документе с диаграммами.

> **Типичное упрощение, которого стоит избегать:** *«Архитектура — это диаграмма, которую рисуют в начале проекта»*. Систему можно спроектировать архитектурно неряшливо при наличии красивой диаграммы — и, наоборот, принять архитектурно значимое решение, вообще не рисуя диаграмм.

### 4.3. Не любое решение архитектурно значимо

Выбор имени переменной не архитектурно значим. Выбор способа приведения разнородных источников данных к сопоставимому виду — значим, потому что от него зависит каждый последующий компонент, использующий эти данные.

Признаки архитектурной значимости: решение трудно локально отменить; оно влияет на несколько частей системы одновременно; ошибка в нём будет воспроизводиться во всех местах использования; его исправление задним числом требует правки уже работающего кода в нескольких местах, а не в одном.

> **Типичное упрощение, которого стоит избегать:** *«Это всего лишь маленький пилотный скрипт — архитектура здесь ни при чём»*. Размер кода не определяет архитектурную значимость решений внутри него: одна функция нормализации данных может быть значимее целой схемы развёртывания.

### 4.4. Границы компонентов определяют, что можно менять независимо

Граница между компонентами — это граница ответственности: что один компонент обязан гарантировать другому. В коде из [`pilot_code_review.md`](assets/pilot_code_review.md) нормализация зашита прямо внутри функции сравнения — граница между «источником данных» и «логикой сравнения» проведена нечётко, и изменение одного неизбежно требует правки другого.

Контрпример — [`assets/adr_data_normalization.md`](assets/adr_data_normalization.md): нормализация вынесена в отдельный явный модуль с метаданными каждого источника (единицы, частота, статус калибровки), и решение принято явно с самого начала, а не обнаружено задним числом при ревью. Хорошо проведённая граница позволяет заменить или расширить источник данных, не трогая остальную систему.

### 4.5. Свойства качества должны быть явными до сравнения альтернатив

Так же как в лекции 2 для сравнения альтернатив решения нужны явные критерии, для сравнения архитектурных альтернатив нужны явные свойства качества: что важнее для этого инкремента — скорость реализации, простота, возможность добавить новый источник данных позже, прозрачность предположений о данных для стороннего проверяющего?

Для инкремента экоцентра расширяемость на новые источники данных существенна: команда уже знает из лекции 2, что вопрос об источниках снова всплывёт, когда встанет вопрос об общественном совете и публичной прозрачности.

### 4.6. ADR фиксирует решение — но не легитимизирует непроверенную догадку

Запись архитектурного решения — не бюрократический ритуал, а способ сделать решение видимым: какая проблема решалась, какие альтернативы рассматривались, почему выбран именно этот вариант, какие предположения он делает, при каких условиях его нужно будет пересмотреть.

Важная оговорка: ADR делает решение видимым, но не делает его автоматически обоснованным. Коэффициент `0.12`, даже будучи явно и подробно записанным в ADR как предположение, остаётся неподтверждённой предметной гипотезой до тех пор, пока её не проверит специалист, — прозрачность и обоснованность решают разные задачи.

> **Типичное упрощение, которого стоит избегать:** *«ADR нужен только для больших решений в начале проекта»*. Архитектурно значимое решение может возникнуть в середине небольшого пилотного скрипта — именно тогда его легче всего не заметить и не зафиксировать.

> **Ещё одно упрощение:** *«Если решение записано в ADR, оно обосновано»*. ADR документирует, что решение принято сознательно и по какой причине. Он не заменяет проверку предметной гипотезы специалистом.

Для решения о нормализации данных ADR мог бы зафиксировать: грубое сопоставление достаточно для целей пилота; формула и её условность указаны явно как неподтверждённая гипотеза; решение будет пересмотрено при появлении второго недорогого источника данных, при предъявлении результата внешней аудитории или после консультации со специалистом экологической службы.

### 4.7. Эволюционность — не то же самое, что проектирование про запас

Частая ошибка — реагировать на тезис «некоторые решения дорого менять» избыточным проектированием: строить универсальные абстракции для всех мыслимых будущих источников данных и форматов, которых пока нет и, возможно, не будет.

> **Типичное упрощение, которого стоит избегать:** *«Нужно сразу спроектировать гибкую архитектуру для всех будущих требований»*. Это обратная крайность той же ошибки: не всё одинаково важно держать открытым. Эволюционность означает осознанный выбор — какие решения оставить дорогими для изменения, потому что они сейчас не важны, а какие сделать явными и легко пересматриваемыми, потому что они, скорее всего, изменятся.

### 4.8. Почему ревью не заменяется автором кода

> **Типичное упрощение, которого стоит избегать:** *«Раз код написал человек, а не AI, архитектурного риска меньше»*. Как показывает [`pilot_code_review.md`](assets/pilot_code_review.md), спешка человека создаёт ровно те же риски, что и слишком буквальное выполнение задачи инструментом. AI в этой ситуации не источник проблемы, а рецензент, честно называющий и находку, и предел собственной компетенции («это вопрос предметной области, не кода»).

## Контрольные вопросы и задания

1. Дайте рабочее определение архитектуры, не используя слова «диаграмма» или «схема».
2. Разберите [`pilot_code_review.md`](assets/pilot_code_review.md): найдите архитектурно значимое решение самостоятельно, до того как прочитаете часть 2 (ревью AI) — совпадает ли ваша находка с находкой AI?
3. Сравните исходный код и контрпример [`adr_data_normalization.md`](assets/adr_data_normalization.md): какое свойство качества улучшилось и какой ценой?
4. Составьте короткий ADR для найденного вами архитектурно значимого решения: контекст, альтернативы, выбор, обоснование, условия пересмотра. Явно укажите, если решение опирается на непроверенную предметную гипотезу.
5. Приведите собственный пример решения, которое выглядело как рядовой технический выбор, но оказалось архитектурно значимым.
6. Объясните разницу между «эволюционной архитектурой» и «проектированием про запас» на собственном примере.
7. Почему запись решения в ADR не эквивалентна его обоснованности? Свяжите ответ с коэффициентом `0.12` из примера лекции.
